name: test-gke-playlist
systems:
  - name: s1
    host: k8s
    k8s-engine: gke
    base: standard-8-vcpu-latest.yml
    overrides:
      hardware:
        volume-config: 1-ssd-1000
        count: 3
    run: 1
common:
  workload:
    topology:
      file: point-to-point/point-to-point-safe.yml
      variables:
        queues: 1
        consumers: 1
    policies:
      file: mirrored-queue.yml
      variables:
        ha-mode: exactly
        ha-params: 2
    #network:
    #rabbit:
    # configuration:
benchmarks:
  - benchmark:
      - workload:
          topology:
            variables:
              publishers: 1
  - benchmark:
      - workload:
          topology:
            variables:
              publishers: 2
  - benchmark:
      - workload:
          topology:
            variables:
              publishers: 4
  - benchmark:
      - workload:
          topology:
            variables:
              publishers: 8