[
    {rabbit, [
        {% if quorum_commands_soft_limit > 0 %}
            {quorum_commands_soft_limit, {{ quorum_commands_soft_limit }} }
        {% endif %}
    ]},
    {ra, [
        {wal_data_dir, '/{{ wal_volume }}/wal'}
        {% if raft_segment_max_entries > 0 %}
            ,{segment_max_entries, {{ raft_segment_max_entries }}}
        {% endif %}
    ]}
].